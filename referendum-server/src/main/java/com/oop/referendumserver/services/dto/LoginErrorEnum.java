package com.oop.referendumserver.services.dto;

/**
 * Enum representing possible login error types
 * Provides constants for indicating incorrect password or non-existent user during the login process
 */
public enum LoginErrorEnum {
    INCORRECT_PASSWORD,
    NON_EXIST
}